Netmaker has been deployed successfully!

=== Application URLs ===

Dashboard: https://{{ .Values.ingress.hostPrefix.ui }}.{{ .Values.baseDomain }}
API:       https://{{ .Values.ingress.hostPrefix.rest }}.{{ .Values.baseDomain }}


=== Verify Deployment ===

1. Check if all pods are running:
   kubectl get pods -n {{ .Release.Namespace }}

2. Check certificate status:
   kubectl get certificates -n {{ .Release.Namespace }}
   kubectl describe certificate {{ include "netmaker.fullname" . }}-tls-secret -n {{ .Release.Namespace }}

3. Check ingress status:
   kubectl get ingress -n {{ .Release.Namespace }}

{{- if .Values.certManager.enabled }}

=== Cert-Manager ===

ClusterIssuer "{{ .Values.certManager.issuerName }}" has been created.
Check issuer status:
   kubectl get clusterissuer {{ .Values.certManager.issuerName }}
{{- end }}

=== Troubleshooting ===

If certificates are not ready, check cert-manager logs:
   kubectl logs -n cert-manager -l app=cert-manager

If pods are not starting, check logs:
   kubectl logs -n {{ .Release.Namespace }} -l app={{ include "netmaker.fullname" . }}
